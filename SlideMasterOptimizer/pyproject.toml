[project]
name = "slidemasteroptimizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "lxml>=6.0.0",
    "pyside6>=6.11.1",
    "qt6-tools>=6.5.0.1.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
